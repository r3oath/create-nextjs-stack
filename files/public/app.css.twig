{% if tailwind %}
@tailwind base;
@tailwind utilities;
@tailwind components;

/* purgecss start ignore */
{% endif %}
#nprogress {
  pointer-events: none;
}

#nprogress .bar {
  background: #FF4270;
  height: 3px;
  left: 0;
  position: fixed;
  top: 0;
  width: 100%;
  z-index: 1031;
}

#nprogress .peg {
  box-shadow: 0 0 10px #FF4270, 0 0 5px #FF4270;
  display: block;
  height: 100%;
  opacity: 1;
  position: absolute;
  right: 0px;
  transform: rotate(3deg) translate(0px, -4px);
  width: 100px;
}

#nprogress .spinner {
  display: block;
  position: fixed;
  right: 15px;
  top: 15px;
  z-index: 1031;
}

#nprogress .spinner-icon {
  animation: nprogress-spinner 400ms linear infinite;
  border-left-color: #FF4270;
  border-radius: 50%;
  border-top-color: #FF4270;
  border: solid 2px transparent;
  box-sizing: border-box;
  height: 18px;
  width: 18px;
}

.nprogress-custom-parent {
  overflow: hidden;
  position: relative;
}

.nprogress-custom-parent #nprogress .spinner,
.nprogress-custom-parent #nprogress .bar {
  position: absolute;
}

@keyframes nprogress-spinner {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}
{% if tailwind %}/* purgecss end ignore */{% endif %}
